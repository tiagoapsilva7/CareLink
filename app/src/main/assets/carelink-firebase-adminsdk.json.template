{
  "_comment": [
    "TEMPLATE: not a working credential.",
    "",
    "The app reads a Firebase Admin SDK service-account key from this assets",
    "directory to obtain an OAuth2 access token for the FCM v1 API (see",
    "ChatActivity.sendNotification and CreateVisitReportActivity).",
    "",
    "To run the notification features locally:",
    "  1. Firebase console > Project settings > Service accounts",
    "  2. 'Generate new private key' and download the JSON",
    "  3. Drop it in app/src/main/assets/",
    "  4. Update the filename passed to assetManager.open(...) in",
    "     ChatActivity and CreateVisitReportActivity to match",
    "",
    "SECURITY NOTE: anything in assets/ is packaged into the APK and can be",
    "extracted from it, so a key shipped this way is effectively public. The",
    "correct production design is to move notification sending into a Firebase",
    "Cloud Function and have the client call that instead. This file is",
    "git-ignored so the key never reaches the repository."
  ],
  "type": "service_account",
  "project_id": "YOUR_PROJECT_ID",
  "private_key_id": "YOUR_PRIVATE_KEY_ID",
  "private_key": "-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY\n-----END PRIVATE KEY-----\n",
  "client_email": "firebase-adminsdk-xxxxx@YOUR_PROJECT_ID.iam.gserviceaccount.com",
  "client_id": "YOUR_CLIENT_ID",
  "auth_uri": "https://accounts.google.com/o/oauth2/auth",
  "token_uri": "https://oauth2.googleapis.com/token",
  "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
  "client_x509_cert_url": "https://www.googleapis.com/robot/v1/metadata/x509/YOUR_CLIENT_EMAIL"
}
